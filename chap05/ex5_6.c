#include <stdio.h>

int main(void)
{
	int rank = 2, m = 0;
	
	swich (rank)
	{
	case 1:
		
	char size;
	
	if (chest <= 90)
	{
	    size = 'S';
	}
	eise if
	printf("a : %d, b : %d\n", a, b);
	
	return 0;
}
